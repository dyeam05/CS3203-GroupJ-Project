// Domain/User.js

class User {
  constructor(accountId, name, email, school) {
    this.accountId = AccountId;   // e.g. "234g324j32"
    this.name = name;   // e.g. "Cayden"
    this.email = email; // e.g. "cayden@mail.com"
    this.school = school; // e.g. "University of Oklahoma"
  }
}

export default CalendarEvent;